import { createStyleSheet } from '@/helpers';

export const getStyles = createStyleSheet(() => ({
  container: {
    flexGrow: 1,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
}));
